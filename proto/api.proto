syntax = "proto3";

package api;

option go_package = "gen/pb";

import "posts.proto";

service ApiService {
    rpc GetPost(Posts.CreateRequest) returns (Posts.CreateResponse) {
        option (google.api.http) = {
            post: "/api/v1/posts/new";
            body: "*";
        };
    }
}